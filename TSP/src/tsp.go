package main

import (
//	"fmt"
)


func main() {
	//~ s := createStopsFromList([]int{0, 1, 2})
	//~ p := Problem{&[][]int{{0, 1, 2}, {2,0,2}, {1,1,0} }}
	//~ sol := p.solve(s)
	//~ println(sol.id)
	//~ arr := []*Stop{s, s.next, s.next.next}
	//~ swap(s, s.next)
	//~ println(s.start())
	//~ for i, s := range arr {
		//~ fmt.Println(i)
		//~ fmt.Println(s)
		//~ println(s)
	//~ }
}
